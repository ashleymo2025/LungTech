import SwiftUI

struct LungTechLogo: View {
    var body: some View {
        Text("LungTech")
            .font(.system(size: 36, weight: .bold, design: .rounded))
            .foregroundColor(.primary)
            .padding()
    }
}
